const randomProduct = products.getRandomProduct();
products.removeProductById(randomProduct.id);


const productChecked = document.getElementById('input');
productImgTags.addEventListener('click', () => {
    if(productChecked)
};
// //////
while(randomProduct.id === randomProduct2.id ||) {
    randomProduct2 === products.getRandomProduct();
};
      let countedProduct;
      if(Math.random() < .33.3)